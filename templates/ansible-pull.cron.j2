# Cron job to run SSH Keys import script every half hour
TEMP=/home/{{ ssh_user }}/.ansible/tmp
*/30 * * * * {{ ssh_user }} ansible-pull -d /home/{{ ssh_user }}/repo --only-if-changed -U https://github.com/studio24/ssh-keys.git >> {{ ansible_pull_log }} 2>&1
